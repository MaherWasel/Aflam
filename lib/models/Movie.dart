class Movie {
  final id;
  final isAdult;
  final orginalTitleText;
  final imageUrl;
  final IMDBRate;
  final worldRank;

  final releaseYear;
  final plot;
  Movie({required this.id,required this.isAdult, required this.orginalTitleText,
  required this.imageUrl,required this.IMDBRate,required this.worldRank,
  required this.releaseYear,required this.plot});
}